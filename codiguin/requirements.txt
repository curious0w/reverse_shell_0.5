
# Requirements for running `main.py`
#
# `teste.py` uses only Python standard library modules (no third-party packages
# are required to run the script). Ensure you use an appropriate Python version
# (Python 3.8+ recommended).
#
# Optional development/test tools to install locally (not required to run the
# script):
pytest>=8.0.0
flake8>=6.0.0
mypy>=1.10.0
psutil>=5.9.0


